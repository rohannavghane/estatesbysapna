import property from './property';
import neighborhood from './neighborhood';
import siteConfig from './siteConfig';
import project from './project';

export const schemaTypes = [property, neighborhood, siteConfig, project];
